# Mobile Driver App

This directory is intended for the Flutter application for drivers.
The `flutter` CLI was not found in the path, so the project could not be automatically initialized.

To initialize:
1. Install Flutter SDK.
2. Run `flutter create .` in this directory.
